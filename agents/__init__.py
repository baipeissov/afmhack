"""
СУНКАР — агентный слой над пайплайном АФМ.

Пакет навешивает мультиагентную координацию поверх существующего анализа
(pipeline/ + models/ + api/dossier.py). Единый источник истины — data/*.jsonl
(risk_queue.jsonl и пр.). LLM-агенты ходят в OpenRouter (бесплатные модели).
"""
